#!/usr/bin/node
let first_arg = Number(process.argv[2])
if (isNaN(parseInt(process.argv[2]))) {
  console.log('Missing size');
} else {
  for (let i = 0; i < process.argv[2]; i++) {
    console.log('X'*first_arg);
  }
}
